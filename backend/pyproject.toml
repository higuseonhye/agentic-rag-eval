[project]
name = "agentic-rag-eval-backend"
version = "0.1.0"
description = "Adaptive Agentic Document Workflow (ADW) backend: routing, workflows, tracing, evaluation, retrieval observability."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

